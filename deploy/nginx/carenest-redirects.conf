# Loaded from http{} via /etc/nginx/conf.d/carenest-redirects.conf
# Maps legacy URLs to primary CareNest destinations (301).
map $uri $carenest_redirect {
    default "";
    include /etc/nginx/snippets/carenest-redirects.map;
}
