# SSL files (not committed)

Place certificates here on the server only:

- Certbot: `/etc/letsencrypt/live/carenesthomehealth.in/`
- Cloudflare origin: `/etc/ssl/cloudflare/carenest.pem` + `carenest.key`

Never commit private keys to GitHub.
